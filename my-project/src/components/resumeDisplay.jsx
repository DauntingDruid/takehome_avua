import { Box } from '@mui/material'
import React from 'react'

export const ResumeDisplay = ({userInfo}) => {
  
    return (
    <Box sx={{minWidth: '100%', minHeight:'100%'}}>
        
        ResumeDisplay
    </Box>
  )
}
